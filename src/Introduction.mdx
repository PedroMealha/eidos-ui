import { Meta, Source } from '@storybook/addon-docs/blocks';
import { GuideHero, GuideStep, GuideSteps, GuideNote } from './guide-page.docs';
import {
  SnackbarSetup,
  DropdownSetup,
  ICONS_EXAMPLE,
  PEER_DEPS_EXAMPLE,
  THEMING_EXAMPLE,
  DEEP_IMPORTS_EXAMPLE,
} from './Introduction.docs';

<Meta title="Getting Started" />

{/* Prose blocks below are div, not p, and must stay that way. MDX parses a */}
{/* multi-line JSX element's children as markdown flow content and wraps */}
{/* them in a p of its own - inside a p that is invalid, and React refuses */}
{/* to hydrate it ("In HTML, p cannot be a descendant of p"). 13 blocks on */}
{/* this page hit it at once. A single-line element keeps its children */}
{/* inline and is also fine, but only until someone's editor wraps the */}
{/* line, so div is the form that stays correct. */}

{/* ── Hero ─────────────────────────────────────────────────────────── */}

<GuideHero title="Getting Started">
  Install the package, load the stylesheet, and wire up the two context providers. Five minutes,
  start to finish.
</GuideHero>

{/* ── Timeline steps ───────────────────────────────────────────────── */}

<GuideSteps>

{/* Step 1 */}

<GuideStep n={1} title="Install">
  <Source dark language="bash" code="npm install eidos-ui" />
</GuideStep>

{/* Step 2 */}

<GuideStep n={2} title="Import the stylesheet">
  Once at your app root - `main.tsx` or equivalent. Without it, components render unstyled. The
  second import is optional: it supplies the bundled Plus Jakarta Sans and JetBrains Mono, which
  the theme names by default but which a browser can only use if something provides the files.

  <Source
    dark
    language="ts"
    code={`import 'eidos-ui/styles';\nimport 'eidos-ui/fonts'; // optional`}
  />
</GuideStep>

{/* Step 3 */}

  <GuideStep n={3} title="Set up providers">
    Most components work standalone. Two require a context provider at your app root.

<SnackbarSetup />

<DropdownSetup />

  </GuideStep>

</GuideSteps>

{/* ── Reference notes ─────────────────────────────────────────────── */}

<GuideNote title="TypeScript" hint="all components are fully typed">
  Import types directly from the main entry point:

  <Source
    dark
    language="ts"
    code={`import type { ButtonProps, InputProps, SelectOption } from 'eidos-ui';`}
  />
</GuideNote>

<GuideNote title="Icons" hint="bundled automatically">
  Many components accept icon props (e.g. `preIcon`, `posIcon`). These use `lucide-react`, which is
  listed as a regular dependency and auto-installed with the package - no extra install step
  needed.

  <Source dark language="tsx" code={ICONS_EXAMPLE} />
</GuideNote>

<GuideNote title="Peer dependencies" hint="not bundled">
  `react` and `react-dom` must be present in your project. React 18 and 19 are both supported.

  <Source dark language="json" code={PEER_DEPS_EXAMPLE} />
</GuideNote>

<GuideNote title="Theming" hint="CSS custom properties">
  All design tokens are CSS custom properties defined on `:root`. Override them in your own
  stylesheet after the library import to customise colours, spacing, border-radius, and more.

  <Source dark language="css" code={THEMING_EXAMPLE} />
</GuideNote>

<GuideNote title="Deep imports" hint="per-component entry points">
  The root barrel is tree-shaken by bundlers that support it. For environments that do not (e.g.
  Jest without transforms, some CJS setups), use the component-level deep import to load only that
  chunk.

  <Source dark language="ts" code={DEEP_IMPORTS_EXAMPLE} />
</GuideNote>
