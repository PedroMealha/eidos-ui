import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as ToolbarStories from './Toolbar.stories';

<Meta of={ToolbarStories} />

# Toolbar

Top application bar pairing an optional brand (avatar + name) with a set of right-aligned action buttons. Used as the top-level bar in `PageLayout`, but usable on its own as a page or section toolbar.

> **Note**: `actions` accepts a restricted subset of `Button` props (`icon` for an icon-only action, or `children`/`preIcon`/`posIcon` for a labeled one) rather than the full `ButtonProps` API - see `ToolbarActionProps`.

## Usage

```tsx
import { Toolbar } from '@pmealha/eidos-ui';

<Toolbar
  brandName="Acme"
  avatar={{ name: 'Acme' }}
  actions={[
    { icon: 'Bell', color: 'secondary', onClick: () => {} },
    { children: 'Settings', preIcon: 'Settings', onClick: () => {} },
  ]}
/>;
```

## Default

<Canvas of={ToolbarStories.Default} />

<Controls of={ToolbarStories.Default} />
