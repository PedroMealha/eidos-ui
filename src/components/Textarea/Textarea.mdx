import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as TextareaStories from './Textarea.stories';

<Meta of={TextareaStories} />

# Textarea

Multi-line text input. Supports labels, character count, helper/error text, loading state, and resize control.

## Usage

```tsx
import { Textarea } from 'eidos-ui';

<Textarea label="Description" placeholder="Enter a description…" rows={4} />;
```

## Playground

<Canvas of={TextareaStories.Playground} />

<Controls of={TextareaStories.Playground} />

## Disabled

<Canvas of={TextareaStories.Disabled} />

## Full width

<Canvas of={TextareaStories.FullWidth} />

## Loading

The dashed border loading state disables user interaction while async work is in progress.

<Canvas of={TextareaStories.Loading} />

## Outlined

<Canvas of={TextareaStories.Outlined} />

## Resize none

<Canvas of={TextareaStories.ResizeNone} />

## Sizes

<Canvas of={TextareaStories.Sizes} />

## With character count

Set `showCount` and `maxLength` together to display a live character counter below the field.

<Canvas of={TextareaStories.WithCount} />

## With disclaimer

An icon button next to the label surfaces important contextual information in a tooltip.

<Canvas of={TextareaStories.WithDisclaimer} />

## With error

<Canvas of={TextareaStories.WithError} />

## With label

<Canvas of={TextareaStories.WithLabel} />
