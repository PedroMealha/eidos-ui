import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as PaginationStories from './Pagination.stories';

<Meta of={PaginationStories} />

# Pagination

Page navigation control for lists and tables. Shows first/last, previous/next, and a window of page number buttons.

> **Note**: `Pagination` is always controlled - you must manage `page` state externally and pass `onChange`.

## Usage

```tsx
import { Pagination } from 'eidos-ui';
import { useState } from 'react';

function MyComponent() {
  const [page, setPage] = useState(1);

  return <Pagination page={page} totalPages={20} onChange={setPage} />;
}
```

## Playground

<Canvas of={PaginationStories.Playground} />

<Controls of={PaginationStories.Playground} />

## Colors

<Canvas of={PaginationStories.Colors} />

## Disabled

<Canvas of={PaginationStories.Disabled} />

## Many pages

Ellipsis is inserted on either side of the page window when the total exceeds what can be shown - `siblingCount` controls how many page buttons appear around the current page.

<Canvas of={PaginationStories.ManyPages} />

## Without first/last

Set `showFirstLast={false}` to remove the ⟨⟨ / ⟩⟩ jump buttons for a more compact layout.

<Canvas of={PaginationStories.NoFirstLast} />

## Sizes

<Canvas of={PaginationStories.Sizes} />
