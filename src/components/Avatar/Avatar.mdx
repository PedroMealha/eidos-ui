import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as AvatarStories from './Avatar.stories';

<Meta of={AvatarStories} />

# Avatar

Displays a user's profile picture, initials, or a fallback icon. When no `src` is provided, the component auto-derives initials and a background color from the `name` prop. Use `AvatarGroup` to stack multiple avatars with an overflow counter.

## Usage

```tsx
import { Avatar, AvatarGroup } from 'eidos-ui';

// Image avatar
<Avatar src="https://example.com/photo.jpg" alt="Jane Smith" size="md" />

// Initials fallback - color is auto-derived from the name
<Avatar name="Jane Smith" size="md" />

// Group with overflow counter
<AvatarGroup max={4}>
  <Avatar name="Alice Johnson" />
  <Avatar name="Bob Smith" />
  <Avatar name="Carol White" />
  <Avatar name="David Brown" />
  <Avatar name="Eve Davis" />
</AvatarGroup>
```

## Playground

<Canvas of={AvatarStories.Playground} />

<Controls of={AvatarStories.Playground} />

## Clickable

Passing `onClick` gives the avatar button semantics and a visible focus ring for keyboard users.

<Canvas of={AvatarStories.Clickable} />

## Colors

<Canvas of={AvatarStories.Colors} />

## Fallback

When neither `src` nor `name` is provided, a generic person icon is rendered.

<Canvas of={AvatarStories.Fallback} />

## Group

`AvatarGroup` stacks avatars with a configurable `max` - avatars beyond the limit are collapsed into a count badge.

<Canvas of={AvatarStories.Group} />

## Group sizes

<Canvas of={AvatarStories.GroupSizes} />

## Shapes

<Canvas of={AvatarStories.Shapes} />

## Sizes

`sm`, `md` and `lg` (32, 40 and 48px) match every other sized control, so an avatar lines up with the buttons and inputs beside it. `xl` (64px) and `2xl` (96px) are for identity media - the avatar leading a profile or account header, where 48px reads as a list thumbnail.

<Canvas of={AvatarStories.Sizes} />

## With custom color

Override the auto-derived background color using the `color` prop.

<Canvas of={AvatarStories.WithCustomColor} />

## Image error

If `src` fails to load, the component falls back to initials (when `name` is set) or the generic icon.

<Canvas of={AvatarStories.WithError} />

## With initials

When `name` is provided and `src` is absent, the component renders initials with an auto-derived background color.

<Canvas of={AvatarStories.WithInitials} />
